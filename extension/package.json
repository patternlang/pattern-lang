{
  "name": "pattern-lang-vscode",
  "displayName": "Pattern Language Support",
  "description": "Language support for Pattern language with LSP",
  "version": "0.0.1",
  "publisher": "pattern-lang",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:csharp"
  ],
  "contributes": {
    "languages": [
      {
        "id": "csharp",
        "extensions": [
          ".cs"
        ]
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package:lsp": "node -e \"const fs=require('fs'); const path=require('path'); const dest=path.join(__dirname,'server'); if(!fs.existsSync(dest)) fs.mkdirSync(dest,{recursive:true}); const patterns=['lsp-csharp','lsp-csharp.exe']; const searchDirs=[path.join(__dirname,'..'),path.join(__dirname,'..','bin'),path.join(__dirname,'..','build')]; for(const dir of searchDirs){if(fs.existsSync(dir)){const files=fs.readdirSync(dir,{recursive:true,withFileTypes:true}).filter(f=>f.isFile()&&patterns.some(p=>f.name===p)); for(const file of files){const src=path.join(file.path||dir,file.name); const dst=path.join(dest,file.name); fs.copyFileSync(src,dst); fs.chmodSync(dst,0o755); console.log('Copied',src,'to',dst);}}}\"",
    "package:vsix": "vsce package",
    "deploy:local": "code --install-extension pattern-lang-vscode-*.vsix --force",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.75.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pattern-lang/pattern-lang-vscode"
  }
}
